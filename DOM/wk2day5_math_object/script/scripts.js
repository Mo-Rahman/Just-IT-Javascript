//Preconditions: Takes two parameters, thh lowest number min and a highest number max
//Postconditions: Returns a random number from min to max.
function getRndInteger(min, max) {
  return Math.floor(Math.random() * (max - min + 1)) + min;
}



array.forEach((element) => {
  element.addEventlister("click", function () {});
});

array.forEach((element) => {
  element.addEventlister("click", function () {
    // Do something here!
  });
});

// - 2 input boxes one for min number one for max
// - Button with click event to trigger function
// - Displays a random number on screen between min and max
